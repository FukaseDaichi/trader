# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# macOS
*.DS_Store

# Virtual environments
.venv

# Environment variables (secrets)
.env

# Curation candidate warmup data (re-fetched each run; not committed to avoid bloat)
data/watchlist/

# Claude Code personal settings (machine-specific; use .claude/settings.json for shared config)
.claude/settings.local.json
